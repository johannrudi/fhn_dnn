inverse map:
- add fourier transform to data loader
- add noise to spike rate and duration
- run test with german's data set
- train cnn autoencoder on small data set, then embedding to param estimation on larger data set
